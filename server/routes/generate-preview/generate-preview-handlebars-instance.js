const generatePreviewHbsInstance = require("../../helpers/common-handlebars-instance");

const generatePreviewTemplate = require("../../template/generate-preview/generate-preview.js.hbs");

module.exports = {
	generatePreviewHbsInstance,
	generatePreviewTemplate,
};
